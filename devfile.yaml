schemaVersion: 2.2.2
metadata:
  name: ai-code-template
  displayName: AI-Powered Python Workspace
  description: >
    Python development environment with Cline, OpenCode, and Task Manager
    pre-configured for your LLM endpoint.
  tags:
    - Python
    - AI
    - Cline
    - OpenCode
  language: Python

# ─────────────────────────────────────────────────────────────────────────────
# EXTENSIONS
#
# Listed via che-code.eclipse.org/vscode-extensions on the dev component.
# The DevWorkspace operator installs them before VS Code starts, so no
# registry redirect or autoApplyRecommendations timing issues.
# Also declared in .vscode/extensions.json for local clone convenience.
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# LLM CONFIGURATION
#
# LLM_API_BASE_URL and LLM_API_KEY are injected via a DevSpaces user secret.
# Create the secret in your <username>-devspaces namespace — see README.md.
# The postStart script rewrites .vscode/settings.json from env vars so
# Cline picks up the correct endpoint on every workspace start.
# ─────────────────────────────────────────────────────────────────────────────

components:
  - name: dev
    container:
      image: registry.redhat.io/devspaces/udi-rhel8:latest
      memoryLimit: 4Gi
      memoryRequest: 1Gi
      cpuLimit: "2"
      cpuRequest: 500m
      mountSources: true
      env:
        - name: LLM_API_BASE_URL
          value: "https://api.openai.com/v1"
        - name: LLM_MODEL_ID
          value: "gpt-oss-120b"
      endpoints:
        - name: flask
          targetPort: 8080
          exposure: public
          protocol: https
    attributes:
      che-code.eclipse.org/vscode-extensions:
        - saoudrizwan.claude-dev
        - cnshenj.vscode-task-manager
        - sst-dev.opencode

commands:
  - id: install-opencode
    exec:
      label: "Install opencode CLI (glibc build)"
      component: dev
      commandLine: |
        set -e
        if [ -x /projects/.opencode/bin/opencode ] && /projects/.opencode/bin/opencode --version >/dev/null 2>&1; then
          echo "opencode already installed: $(/projects/.opencode/bin/opencode --version)"
        else
          echo "Installing opencode (glibc build)..."
          curl -fsSL https://opencode.ai/install | bash -s -- --no-modify-path
          mkdir -p /projects/.opencode/bin
          cp "$HOME/.opencode/bin/opencode" /projects/.opencode/bin/opencode
          chmod +x /projects/.opencode/bin/opencode
          echo "Done — opencode $(/projects/.opencode/bin/opencode --version) installed."
        fi

  - id: setup-workspace
    exec:
      label: "Setup: configure LLM endpoint from env vars"
      component: dev
      commandLine: |
        set -e
        export PATH="/projects/.opencode/bin:$PATH"
        grep -q '/projects/.opencode/bin' "$HOME/.bashrc" 2>/dev/null || \
          echo 'export PATH="/projects/.opencode/bin:$PATH"' >> "$HOME/.bashrc"
        echo "Writing .vscode/settings.json from env vars..."
        mkdir -p ${PROJECT_SOURCE}/.vscode
        python3 -c "import json,os; s={'extensions.autoApplyRecommendations':True,'cline.apiProvider':'openai-compatible','cline.openAiBaseUrl':os.environ.get('LLM_API_BASE_URL','https://api.openai.com/v1'),'cline.openAiModelId':'gpt-oss-120b'}; open('${PROJECT_SOURCE}/.vscode/settings.json','w').write(json.dumps(s,indent=2))"
        echo "Done."

  - id: run-app
    exec:
      label: "Run Flask App"
      component: dev
      commandLine: "pip install -r requirements.txt && python app.py"
      workingDir: ${PROJECT_SOURCE}
      group:
        kind: run
        isDefault: true

  - id: opencode-run
    exec:
      label: "Launch OpenCode (terminal AI)"
      component: dev
      commandLine: "export PATH=/projects/.opencode/bin:$PATH && opencode"
      workingDir: ${PROJECT_SOURCE}

events:
  postStart:
    - install-opencode
    - setup-workspace
